-- Escreva uma query que aplica os apelidos `primeiro_nome` e `ultimo_nome` 
-- respectivamente para as colunas `first_name` e `last_name` da tabela `users`;
SELECT first_name AS primeiro_nome, last_name AS ultimo_nome
FROM hotel_database.users
